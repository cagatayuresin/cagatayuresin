name: Latest Blog Posts

on:
  schedule:
    - cron: "0 4 * * *" # her gün 04:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Pull latest posts from RSS
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          feed_list: "https://cagatayuresin.com/rss.xml"
          max_post_count: 5
          commit_message: "docs(readme): update latest blog posts"